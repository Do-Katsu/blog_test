{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <div class="jumbotron">
        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    <img class="img-fluid" src="{{ article.featuredImage }}" alt="{{ article.featuredText }}">
                </div>
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm">
                        <div class="card-header">
                            <h4 class="my-0 font-weight-normal">{{ article.title }}</h4>
                        </div>
                        <div class="card-body">
                            <p class="h1 card-title pricing-card-title">
                                 €
                            </p>
                            <button class="btn btn-lg btn-block btn-outline-primary" type="button">Contacter le vendeur</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row mt-4">
            <div class="col-md-12">
                <h2>Contenu</h2>
                <p>
                    {{ article.content }}
                </p>
            </div>
        </div>
    </div>

    <a href="{{ path('app_article_index') }}">back to list</a>

    <a href="{{ path('app_article_edit', {'id': article.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }}
    {% include 'article/comments.html.twig' %}
{% endblock %}
